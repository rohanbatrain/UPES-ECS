; UPES-ECS RTP media port range.
; Kept small + fixed so it can be port-forwarded (van-laptop / behind-NAT deployments).
; 20 ports ~= up to 20 concurrent call legs. Widen for production (e.g. 10000-10500)
; and forward the matching range on the router/host.
[general]
rtpstart=10000
rtpend=10019
